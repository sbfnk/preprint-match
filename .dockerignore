.git
.venv
venv
__pycache__
*.pyc
xml/
embeddings/
doi_to_xml.json
results/
# Model and adapter directories are inputs to precompute, never copied into
# the image. The refresh workflow unpacks them into the repo root, so without
# these the build context carries ~400MB of dead weight to the remote builder
# on every deploy. Wildcards so a new version does not silently slip back in.
finetuned-specter2*/
model/
model-v*/
*.tar.bz2
*.tar.gz
*.tar
labeled_*.json
!labeled_dataset_slim.json
*_progress*.jsonl
*.npy
*.pkl
*.parquet
predictions/papers_old.json
predictions/papers.json
predictions/new_papers.json
predictions/embeddings.npz
predictions/new_embeddings.npz
predictions/journal_rankings.json
